[project]
name = "imgflip-scraper"
version = "0.1.0"
description = "Scrapes blank meme templates from Imgflip for the semiotic_harm pipeline"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "beautifulsoup4>=4.12.0",
]